task_007 検証ログ（2026-09-04）
iOS 26.x シミュレータランタイムが未導入のため、scripts/build-ios.sh / test-ios.sh は通らない。
代替として swiftc の型検査を行う。リンク・実行・実機動作は未検証。

==================================================================
1) SDK
$ xcrun --sdk iphonesimulator --show-sdk-path
/Applications/Xcode.app/Contents/Developer/Platforms/iPhoneSimulator.platform/Developer/SDKs/iPhoneSimulator26.2.sdk

==================================================================
2) 型検査（ファイル単位）
$ swiftc -typecheck -swift-version 6 -strict-concurrency=complete -sdk "$SDK" -target arm64-apple-ios26.0-simulator App/Audio/*.swift
EXIT=0

==================================================================
3) 型検査（Saydo ターゲットと同じフラグ。project.yml の
   SWIFT_UPCOMING_FEATURE_EXISTENTIAL_ANY と -enforce-exclusivity=checked を追加）
$ swiftc -typecheck -swift-version 6 -strict-concurrency=complete -enable-upcoming-feature ExistentialAny -enforce-exclusivity=checked -sdk "$SDK" -target arm64-apple-ios26.0-simulator App/Audio/*.swift
EXIT=0

==================================================================
4) whole-module（-typecheck はファイル単位のため、region 解析の診断は
   whole-module でないと出ない。実際にこの段でだけ 1 件見つかり修正した）
$ swiftc -emit-module -module-name Saydo -enable-testing -swift-version 6 -strict-concurrency=complete -sdk "$SDK" -target arm64-apple-ios26.0-simulator -emit-module-path <tmp>/Saydo.swiftmodule App/SaydoApp.swift App/Audio/*.swift
EXIT=0

==================================================================
5) テストの型検査（4) の .swiftmodule に @testable import Saydo を解決させる）
$ swiftc -typecheck -swift-version 6 -strict-concurrency=complete -sdk "$SDK" -target arm64-apple-ios26.0-simulator -I <tmp> -I "$PLAT/Developer/usr/lib" -F "$PLAT/Developer/Library/Frameworks" Tests/SaydoTests/SilenceDetectorTests.swift
EXIT=0

   逆対照（この検査が実際に Saydo モジュールを見ていることの確認）:
   hasFinished を存在しない名前へ置換したコピーを同じコマンドに掛けると
   error: value of type 'SilenceDetector' has no member 'doesNotExistAtAll' が出て落ちる。

==================================================================
6) 警告件数
$ grep -c "warning:" <2)〜5) の出力>
0

==================================================================
7) scripts/lint-principles.sh
lint-principles: 対象 8 ファイル（App/ と Packages/*/Sources。Tests と Spikes は除外）
lint-principles: OK
EXIT=0

==================================================================
8) scripts/test-ios.sh（環境要因で未達）
test-ios: iOS 26.x の利用可能なシミュレータが見つからない。
  導入コマンド: xcodebuild -downloadPlatform iOS（約 8.4 GB の空きが必要）
  現在のランタイム一覧:
== Runtimes ==
iOS 18.5 (18.5 - 22F77) - com.apple.CoreSimulator.SimRuntime.iOS-18-5
EXIT=2

==================================================================
9) scripts/build-ios.sh（環境要因で未達。Swift のコンパイルまで到達しない）

** BUILD FAILED **

The following build commands failed:
	CompileAssetCatalogVariant thinned .../build/Debug-iphonesimulator/Saydo.app .../App/Resources/Assets.xcassets (in target 'Saydo' from project 'Saydo')
(1 failure)
EXIT=65

エラー本文:
    App/Resources/Assets.xcassets: error: No simulator runtime version from ["22F77"]
    available to use with iphonesimulator SDK version 23C53

重要（誤読しないための記録）:
この失敗は actool（アセットカタログ）だけで、**Swift のコンパイルには一度も到達していない**。
検証のため一時的に `xcodegen generate` して App/Audio の 7 ファイルをターゲットへ入れた状態でも、

    $ grep -cE "^SwiftCompile |Compiling " <build ログ>   → 0
    $ grep -cE "EmitSwiftModule" <build ログ>              → 0
    $ ls build/Saydo.build/.../Objects-normal/arm64/*.o    → 0 個

だった（SwiftFileList には App/Audio の 7 ファイルが入っていた）。
したがって **xcodebuild は本タスクのコードについて何も検証していない**。
コードの検証は上の 2)〜5) の swiftc だけが根拠である。
確認後、一時生成した Saydo.xcodeproj は `git checkout -- Saydo.xcodeproj` で戻し、build/ は削除した。
