# 引き継ぎ（2026-09-04 05:52、Fable → Opus）

## 状態
- main: 計画 7 文書（implementation-plan / task-list 23 件 / acceptance-checks 39 件 / harness-design / retention-strategy / monetization / review/fix-decisions）を敵対的レビュー反映済みで push 済み。反証検証で格下げされた 8 件の「継ぎ目」は Opus エージェントが docs に適用中（完了後: JSON 妥当性確認 → commit → push）。
- ブランチ `task/001-bootstrap`（push 済み）: task_001（dbeff9c）と task_002（76eb269）完了。`scripts/test-core.sh` 緑（15 tests）。
- 並行中の worktree（全て Opus、`.worktrees/` 配下）: task/003-fm-probe、task/004-speech-spike、task/005-flows（task_005 + 005b）、task/006-data、task/009-notification-core（NotificationPlan / Copy + InsightCalculator）。UI デザインは docs/design/ に作成中。
- マージ順: 001 → 005 → 006 → 009core → 004 → 003。各ブランチは `Workflow({name: "task-review", args: {base: "main", task_id: "task_005"}})` を通してから main にマージする。

## ブロッカー（人間の作業）
1. **iOS 26.x シミュレータランタイム未導入**。`xcodebuild -downloadPlatform iOS` が「Insufficient space available. Requires 8.39 GB」で失敗（空き 6.0 GiB）。Xcode のキャッシュ類は合計 60 MB 未満で削っても足りない。**約 3 GB 以上を別の場所で空けてから再実行**する。これが解けるまで `scripts/build-ios.sh` / `scripts/test-ios.sh` は通らない（SaydoCore の `swift test` と macOS の fm-probe は動く）。
2. Foundation Models の実行には Mac の Apple Intelligence 有効化が必要（task_003 の結果を参照）。
3. task_004 の実機計測（docs/spikes/speech-spike.md の記入欄）。

## Opus セッションが次にやること
1. 未完了エージェントの完了通知を待ち、各ブランチの `docs/PROGRESS.md` と検証出力を確認する（捏造チェック: exit code と末尾ログが貼られているか）。
2. `.xcodeproj` は `.gitignore` へ（生成物ポリシー。task_001 のコミットに含まれていれば `git rm --cached` で外し、scripts が `xcodegen generate` を自動実行するよう確認）。
3. task-review → マージ → push。競合は project.yml と Package.swift のみ想定。
4. 方針: 計画・分析は Fable（次の枠）、実装は Opus。企画原則 §22 と Guardrails を弱めない。ネットワーク API を追加しない。

## 追記 06:0x（task_009-core / task_016-core 完了、push 済み）
- ブランチ `task/009-notification-core`（7e080ff, 4f912f6）。test-core 62 テスト成功。
- レビュー要（Opus）: (1) 昼スキップ条件 (b)「now < plannedAt」の実装だと宣言直後の再計画で当日の固定昼通知が常に消える。意図は「昼の発火時刻 < plannedAt の日は昼を出さない」なので、条件を発火時刻基準に直すか判断する。(2) 行動時刻通知の sessionType は .noon。(3) 禁止句リストが NotificationCopyTests に直書き → task_005 の Guardrails 到着後に寄せる。(4) lint の WARN（InsightCopy.swift の日本語リテラル 11 件）は許可リスト追加を判断する。

## 追記（task_004 完了、push 済み）
- ブランチ `task/004-speech-spike`（d2f6495）。`scripts/build-ios.sh SpeechSpike` exit 0（コンパイル・リンクのみ。実行は実機待ち。docs/spikes/speech-spike.md §4 に計測手順と記入欄）。
- 事実確認: `AnalyzerInputConverter` は iOS 26.2 SDK に存在しない（AVAudioConverter + AnalyzerInput(buffer:) で実装）。`finalizeAndFinishThroughEndOfInput()` を使用。`.allowBluetoothHFP` が正式名。`SpeechDetector` モジュールが SDK にあり RMS 無音判定の代替候補。
- scripts/build-ios.sh にランタイム未導入時の `-target` フォールバックを追加（マージ時に main へ取り込む）。
- 新規ファイル追加のため main 取り込み後に `xcodegen generate` が必要。

## 追記（task_006 完了、push 済み）
- ブランチ `task/006-data`（e64542f）。iOS 26 シミュレータ SDK に対する型検査 0 警告、macOS 代替実行で 32 テスト成功。実機・シミュレータ実行は未検証。
- レビュー要: (1) `weekendNotificationsEnabled` の既定は true（R2「週末は既定でオフにできる」の解釈）。(2) `aloneTime` 既定 nil → 夜の時刻にフォールバック。(3) Repository は Sendable スナップショットを返す設計。(4) 新規 12 ファイルは main で `xcodegen generate` 後に反映。

## 追記（task_009-app 完了、push 済み）
- ブランチ `task/009-notification-app`（dc698c1、`task/009-notification-core` から分岐）。iOS 26 SDK 型検査 0 警告（strict concurrency complete）。リンク・実行は未検証。
- 残作業: (1) Time Sensitive エンタイトルメントを project.yml に追加。(2) 通知アクション「今は話せない」（60 分後に 1 件再登録）は SaydoCore の NotificationCopy に定数が無いため未実装 → SaydoCore 側に追加する後続タスク。(3) AppRouter を SessionLauncher に準拠させ AppDelegate.setLauncher に配線（task_008 以降）。(4) main 取り込み後に `xcodegen generate`。

## 追記（task_020 文書 4 本、docs/app-store/ にコミット済み）
- 要確認 10 件は各文書末尾に記載。主要: MARKETING_VERSION 0.1.0 → 1.0.0、プライバシーポリシー公開 URL（non-turn.com 配下、実パス未確定）、販売地域、Time Sensitive エンタイトルメント、設定画面の項目名の実装との一致。task-list.json の task_020 files_to_create に privacy-policy.md と testflight-tester-guide.md を追加すること。

## 追記（task_007 完了、push 済み）
- ブランチ `task/007-audio`（0af6c4f、`task/004-speech-spike` から分岐）。App/Audio 7 ファイル。型検査（-typecheck と -emit-module の両方）0 警告。SilenceDetectorTests は未実行。
- 注意: `-typecheck` はファイル単位で region 解析の診断を取りこぼす。AVAudioPCMBuffer を跨ぐ箇所は `-emit-module` でも検査すること。`.defaultToSpeaker` は付けない設計（R8 のイヤホン優先）。AudioSessionController.deactivate() は SessionViewModel が明示的に呼ぶ。main 取り込み後 `xcodegen generate` 必須。

## 追記（task_019-core 完了、push 済み）
- ブランチ `task/019-data-export`（8a12578、`task/006-data` から分岐）。型検査 0 警告、macOS 実行で 22 件 PASS。iOS 実行・ShareLink・復元は未検証。
- **要修正（task_006 側の不具合）**: `DayKey.make(from:calendar:)` は和暦端末で元号年を返す（2026-09-04 → 0008-09-04）。`Calendar(identifier: .gregorian)` に固定する修正を task_006 のフォローとして入れる。
- UI 側の申し送り: 「全部消す」は `Repository.deleteAll { removeAllPendingNotificationRequests() }` と `AppSettings.shared.reset()` の両方を呼ぶ。

## 追記（task_005 / task_005b 完了、push 済み）
- ブランチ `task/005-flows`（298413b, 7c466c4）。test-core 143 テスト成功（ミューテーションで有効性確認済み）。
- 未達 1 点: copy-audit（Workflow スクリプト）は Opus セッションから `Workflow({name: "copy-audit"})` で実行する。機械判定可能な部分は手動で 0 件確認済み、トーン判定は未検証。
- 引き継ぎ: NotificationCopy（task_009-core）を Guardrails に通すテストは task_005 と 009core のマージ後に追加。週次振り返りテンプレートは TemplateDialogueEngine.swift 内（lint WARN）。JapaneseTimeParser は漢数字未対応。
- マージ順の確定: 001 → 005 → 006 → 009core → 009app → 004 → 007 → 019 → 003 → 014 → 008。
